# Copia este archivo como ".env" (sin ".example") y completa tus datos reales.
# El archivo ".env" NUNCA debe subirse a GitHub (ya está en .gitignore).

DB_USER=postgres
DB_HOST=localhost
DB_NAME=antojitos_dulces
DB_PASSWORD=tu_password_de_postgres
DB_PORT=5432

PORT=3000
